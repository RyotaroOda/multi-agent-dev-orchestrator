# マイルストーン（企画フェーズ）

## M1: 企画の収束条件を確定

- 目的/非目的が合意されている
- MVP範囲が定義されている
- 成功指標（KPI）が定義されている

期限: 未設定
状態: done

## M2: 実行アーキテクチャ方針の確定

- エージェント責務分離が合意されている
- 実行隔離方針（Issue単位）が合意されている
- モデルルーティング方針が合意されている

期限: 未設定
状態: done

## M3: 運用・安全ガードレール確定

- 品質ゲート条件が明文化されている
- リスク対応の優先順位が合意されている
- Blocked時の復帰ルールが合意されている

期限: 未設定
状態: done

## M4: 開発着手判定

- 優先度付きバックログ（最低10件）が ready または done
- 未決事項が許容範囲まで縮小
- 開発開始Go/No-Goが記録済み

期限: 2026-02-21（本判定）
状態: done

## M4以降のマクロ計画（再整理: 2026-02-13）

前提:

- 現行運用は `AGENTS.md` の「企画フェーズ継続」を優先する
- `M4: done` は履歴として維持し、残論点は後続マイルストーンで収束する

## M5: P0仕様不整合の解消完了

- `PLN-045` / `PLN-046` / `PLN-047` / `PLN-048` が `done`
- `R-020` / `R-021` の状態が `open` から遷移している
- `DEV-001` / `DEV-002` の受け入れ条件が仕様横断で一意に説明できる

期限: 2026-02-16（目標）
状態: done

## M6: 実装着手の運用前提を監査可能に固定

- M4履歴と企画継続方針の整合が `意思決定ログ` に記録される
- 着手判断ゲート（進める/止める）がマイルストーン条件で明文化される
- 実装開始前に確認すべき依存関係（認証/語彙/監査出力）が一覧化される

期限: 2026-02-18（目標）
状態: done

## M7: 初回3Issueの着手順再確認と出口条件固定

- Issue `#5 -> #6 -> #7` の順序維持条件と変更条件が明文化される
- 各Issueの「開始条件/中断条件/完了条件」が同一フォーマットで定義される
- 変更時の台帳更新責務（バックログ/リスク/ADR）が固定される

期限: 2026-02-20（目標）
状態: done

## M8: 企画フェーズ最終判定（再Go/No-Go）

- M5〜M7の完了記録が揃っている
- `open` のP0リスクに対する許容判断が記録されている
- 次フェーズ移行条件（実装開始日・担当・監査手順）が明示されている
- 判定記録が `plan/M8再Go-No-Go判定_2026-02-13.md` に保存されている

期限: 2026-02-21（目標）
状態: done

## 実装フェーズ長期計画（追加: 2026-02-13）

前提:

- 本節は「企画フェーズでの実装計画」を示すものであり、現時点で実装コード作成は行わない。
- セキュリティ優先順位 `A:漏えい防止 > B:ホスト保護 > C:課金回避` を維持する。
- 初回着手順は `#5 -> #6 -> #7` を維持し、変更時はADR追記を必須とする。

## M9: 実装フェーズ移行ゲートの再確認

- `specs/13 -> specs/15 -> specs/16 -> specs/17 -> specs/18` の参照順で着手前レビューが完了している
- `dev-env-check` 前提（コマンド、鍵、`GH_TOKEN`）の不足項目が解消されている
- 着手ログの記録先（`バックログ`/`リスク登録簿`/`意思決定ログ`）の担当が固定されている

期限: 2026-02-20（目標）
状態: ready

## M10: 初回3Issue（DEV-001〜003）実装完了

- Issue `#5/#6/#7` の受け入れ条件を満たし、`Commit -> PR -> Merge -> Done` が完了している
- `run_id` / `app_slug` / 例外ラベル4項目の監査出力がIssue起点で追跡できる
- `blocked` 発生時の復帰記録が `blocked_reason` 命名規約と一致している

期限: 2026-03-06（目標）
状態: todo

## M11: AI監査ジョブの実運用開始

- `Collector/Evaluator/Reporter/Dispatcher` が仕様契約どおりに実装されている
- 週次監査（毎週金曜）と設定変更日の臨時監査が運用記録として残る
- 監査失敗時に fail-closed（`run_control=blocked`）と同日再実行が実施される

期限: 2026-03-13（目標）
状態: todo

## M12: v0.2統合安定化

- KPI（throughput/merge率/blocked率/run時間）が目標範囲内で2週連続維持される
- P0リスクが `watching` 以下へ遷移し、未解消項目の許容判断が記録される
- 運用手順（非常時運用含む）が最新状態で整合し、監査で差分ゼロを確認できる

期限: 2026-03-20（目標）
状態: todo

## M13: v0.2運用定着判定

- 初回実装サイクルのふりかえりが完了し、次スプリントの改善バックログが確定している
- 週次監査と日次レビューの定着率が合意水準を満たしている
- v0.3で扱う拡張論点（署名検証、DGX最適化など）が分離記録されている

期限: 2026-03-27（目標）
状態: todo
