# M5完了判定チェック表（2026-02-13）

## 1. 目的

`M5: P0仕様不整合の解消完了` の判定条件を1枚に固定し、`PLN-045〜048` と `R-020/R-021` の完了判定を一意化する。

## 2. 判定条件

| 判定ID | 判定条件 | 参照先 | 判定 |
| --- | --- | --- | --- |
| M5-C01 | `PLN-045` が `done` である | `plan/バックログ.md` | 達成 |
| M5-C02 | `PLN-046` が `done` である | `plan/バックログ.md` | 達成 |
| M5-C03 | `PLN-047` が `done` である | `plan/バックログ.md` | 達成 |
| M5-C04 | `PLN-048` が `done` である | `plan/バックログ.md` | 達成 |
| M5-C05 | `R-020` の状態が `open` ではない | `plan/リスク登録簿.md` | 達成 |
| M5-C06 | `R-021` の状態が `open` ではない | `plan/リスク登録簿.md` | 達成 |
| M5-C07 | DEV-001の受け入れ条件を `01/15/18` で一意に説明できる | `plan/specs/01_*.md`, `plan/specs/15_*.md`, `plan/specs/18_*.md` | 達成 |
| M5-C08 | DEV-002の判定境界と例外可否を `05/06/11/15` で一意に説明できる | `plan/specs/05_*.md`, `plan/specs/06_*.md`, `plan/specs/11_*.md`, `plan/specs/15_*.md` | 達成 |

## 3. 判定結果

- 判定日: 2026-02-13
- 総合判定: `M5完了`
- 次ゲート: `M6`（実装着手の運用前提固定）

## 4. 更新ルール

- いずれかの判定IDが未達になった場合、`M5` 状態を `ready` に戻す。
- 判定条件を変更する場合は `plan/意思決定ログ.md` にADRを追記する。
